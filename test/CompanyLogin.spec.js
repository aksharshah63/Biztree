import { mount } from "@vue/test-utils";
import CompanyLogin from "@/pages/login.vue";

describe("CompanyLogin", () => {
  test("is a Vue instance", () => {
    // const wrapper = mount(CompanyLogin);
    // expect(wrapper.vm).toBeTruthy();
  });
});
